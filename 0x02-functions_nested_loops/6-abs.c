#include "main.h"
/**
 * _abs - prints the absolute integer
 *
 * @int: type of a variable
 * Return: always 0
 */
int _abs(int)
{
int ru;

ru = srlen(int);
if (ru > 1)
{
_putchar(srlen(int[1]));
}
return (0);
}
