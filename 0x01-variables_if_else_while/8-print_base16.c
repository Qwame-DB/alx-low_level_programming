#include <stdio.h>
/**
 * main - entry point to the code
 *
 * Description: Base 16 numbers
 * Return: returns 0
 */
int main(void)
{
int a;
for (a = 0; a <= 15; a++)
{
putchar (a);
}
putchar ('\n');
return (0);
}
