#include "main.h"
/**
 * _abs - function for the absolute integer
 *
 * @int: type of a variable
 * Return: always 0
 */
int _abs(int x)
{
if (x < 0)
{
x = x * -1;
}
else
{
x = x * 1;
}
return (0);
}
