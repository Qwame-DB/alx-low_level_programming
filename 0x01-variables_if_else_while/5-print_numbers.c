#include <stdio.h>
/**
 * main - entry point to all of my code
 *
 * Description: prints all base 10 numbers
 * Return: always returns 0
 */
int main(void)
{
int a;
for (a = '0'; a <= '9'; a++)
{
putchar(a);
}
return (0);
}
