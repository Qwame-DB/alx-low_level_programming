#include <stdio.h>
/**
 * main - entry point for my code
 *
 * Description: printing numbers
 * Return: always return 0
 */
int main(void)
{
int num;
for (num = 0; num < 9; num++)
{
putchar(num);
}
putchar('\n');
return (0);
}
